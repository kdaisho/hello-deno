export function Sum({ a, b }: { a: number; b: number }) {
  return a + b;
}

export function GetRecipe() {
  return {
    title: "Curry",
    ingredients: "Red Peppers",
    description: "Yummy.",
  };
}
